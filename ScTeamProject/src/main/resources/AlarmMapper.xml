<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="com.scmaster.mapper.AlarmMapper">

	<select id="selectBabyAlarmList" resultType="Ajax_Alarm" parameterType="Integer"> 
		SELECT ALARMNO AS "ID", ALARMTITLE AS "TITLE", TO_CHAR(ALARMTIME,'YYYY-MM-DD/HH24:MI:SS') AS "START"
		FROM BS_ALARM
		WHERE BABYNO=#{babyNo}
	</select>
	
	<insert id="insertAlam" parameterType="BS_Baby" >
		INSERT INTO BS_ALARM ALARMNO, BABYNO, ALARMTITLE,ALARMTIME,ALARMMEMO
		VALUES (BS_ALARMSEQ.NEXTVAL,
		'${babyno}',
		'${alamTitle}',
		TO_DATE('${alarmTime}','YYYY-MM-DD/HH24:MI:SS'),
		'${alarmMemo}')
	</insert>
	
	
</mapper>